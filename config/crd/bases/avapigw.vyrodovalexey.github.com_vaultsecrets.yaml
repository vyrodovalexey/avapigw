---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: vaultsecrets.avapigw.vyrodovalexey.github.com
spec:
  group: avapigw.vyrodovalexey.github.com
  names:
    kind: VaultSecret
    listKind: VaultSecretList
    plural: vaultsecrets
    shortNames:
    - vs
    singular: vaultsecret
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.lastRefreshTime
      name: LastRefresh
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          VaultSecret is the Schema for the vaultsecrets API.
          VaultSecret defines a reference to a secret stored in HashiCorp Vault.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VaultSecretSpec defines the desired state of VaultSecret
            properties:
              keys:
                description: Keys defines the key mappings from Vault to the target
                  secret.
                items:
                  description: VaultKeyMapping defines a key mapping from Vault to
                    the target secret
                  properties:
                    encoding:
                      allOf:
                      - enum:
                        - None
                        - Base64
                      - enum:
                        - None
                        - Base64
                      default: None
                      description: Encoding defines the encoding of the value.
                      type: string
                    targetKey:
                      description: TargetKey is the key in the target Kubernetes secret.
                      maxLength: 253
                      minLength: 1
                      type: string
                    vaultKey:
                      description: VaultKey is the key in the Vault secret.
                      maxLength: 253
                      minLength: 1
                      type: string
                  required:
                  - targetKey
                  - vaultKey
                  type: object
                maxItems: 32
                type: array
              mountPoint:
                default: secret
                description: MountPoint is the Vault mount point (KV engine).
                maxLength: 253
                minLength: 1
                type: string
              path:
                description: Path is the path to the secret in Vault.
                maxLength: 1024
                minLength: 1
                type: string
              refresh:
                description: Refresh defines the refresh configuration.
                properties:
                  enabled:
                    default: true
                    description: Enabled indicates whether automatic refresh is enabled.
                    type: boolean
                  interval:
                    default: 5m
                    description: Interval is the refresh interval.
                    type: string
                  jitterPercent:
                    default: 10
                    description: JitterPercent is the percentage of jitter to add
                      to the refresh interval.
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                type: object
              target:
                description: Target defines the target Kubernetes secret to create/update.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations are annotations to add to the target
                      Secret.
                    maxProperties: 16
                    type: object
                  creationPolicy:
                    allOf:
                    - enum:
                      - Owner
                      - Orphan
                      - Merge
                    - enum:
                      - Owner
                      - Orphan
                      - Merge
                    default: Owner
                    description: CreationPolicy defines when to create the target
                      Secret.
                    type: string
                  deletionPolicy:
                    allOf:
                    - enum:
                      - Delete
                      - Retain
                    - enum:
                      - Delete
                      - Retain
                    default: Delete
                    description: DeletionPolicy defines what to do with the target
                      Secret when the VaultSecret is deleted.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels are labels to add to the target Secret.
                    maxProperties: 16
                    type: object
                  name:
                    description: Name is the name of the target Secret.
                    maxLength: 253
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the target Secret.
                      If not specified, the namespace of the VaultSecret is used.
                    maxLength: 63
                    minLength: 1
                    type: string
                  type:
                    default: Opaque
                    description: Type is the type of the target Secret.
                    enum:
                    - Opaque
                    - kubernetes.io/tls
                    - kubernetes.io/dockerconfigjson
                    type: string
                required:
                - name
                type: object
              vaultConnection:
                description: VaultConnection defines the Vault connection configuration.
                properties:
                  address:
                    description: Address is the Vault server address.
                    maxLength: 2048
                    minLength: 1
                    type: string
                  auth:
                    description: Auth defines the authentication configuration.
                    properties:
                      appRole:
                        description: AppRole defines AppRole authentication configuration.
                        properties:
                          mountPath:
                            default: approle
                            description: MountPath is the mount path for AppRole auth.
                            maxLength: 253
                            minLength: 1
                            type: string
                          roleId:
                            description: RoleID is the AppRole role ID.
                            maxLength: 253
                            minLength: 1
                            type: string
                          secretIdKey:
                            default: secret-id
                            description: SecretIDKey is the key in the Secret containing
                              the secret ID.
                            maxLength: 253
                            minLength: 1
                            type: string
                          secretIdRef:
                            description: SecretIDRef references a Secret containing
                              the AppRole secret ID.
                            properties:
                              name:
                                description: Name is the name of the secret
                                maxLength: 253
                                minLength: 1
                                type: string
                              namespace:
                                description: |-
                                  Namespace is the namespace of the secret
                                  If not specified, the namespace of the referrer is used
                                maxLength: 63
                                minLength: 1
                                type: string
                            required:
                            - name
                            type: object
                        required:
                        - roleId
                        - secretIdRef
                        type: object
                      kubernetes:
                        description: Kubernetes defines Kubernetes authentication
                          configuration.
                        properties:
                          mountPath:
                            default: kubernetes
                            description: MountPath is the mount path for Kubernetes
                              auth.
                            maxLength: 253
                            minLength: 1
                            type: string
                          role:
                            description: Role is the Vault role to authenticate with.
                            maxLength: 253
                            minLength: 1
                            type: string
                          serviceAccountRef:
                            description: ServiceAccountRef references the ServiceAccount
                              to use for authentication.
                            properties:
                              name:
                                description: Name is the name of the referent
                                maxLength: 253
                                minLength: 1
                                type: string
                            required:
                            - name
                            type: object
                          tokenPath:
                            description: TokenPath is the path to the service account
                              token.
                            maxLength: 1024
                            type: string
                        required:
                        - role
                        type: object
                      token:
                        description: Token defines token authentication configuration.
                        properties:
                          secretRef:
                            description: SecretRef references a Secret containing
                              the Vault token.
                            properties:
                              name:
                                description: Name is the name of the secret
                                maxLength: 253
                                minLength: 1
                                type: string
                              namespace:
                                description: |-
                                  Namespace is the namespace of the secret
                                  If not specified, the namespace of the referrer is used
                                maxLength: 63
                                minLength: 1
                                type: string
                            required:
                            - name
                            type: object
                          tokenKey:
                            default: token
                            description: TokenKey is the key in the Secret containing
                              the token.
                            maxLength: 253
                            minLength: 1
                            type: string
                        required:
                        - secretRef
                        type: object
                    type: object
                  inCluster:
                    default: false
                    description: InCluster indicates whether to use in-cluster Vault.
                    type: boolean
                  namespace:
                    description: Namespace is the Vault namespace (Enterprise only).
                    maxLength: 253
                    type: string
                  tls:
                    description: TLS defines the TLS configuration.
                    properties:
                      caCertRef:
                        description: CACertRef references a Secret containing the
                          CA certificate.
                        properties:
                          name:
                            description: Name is the name of the secret
                            maxLength: 253
                            minLength: 1
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of the secret
                              If not specified, the namespace of the referrer is used
                            maxLength: 63
                            minLength: 1
                            type: string
                        required:
                        - name
                        type: object
                      clientCertRef:
                        description: ClientCertRef references a Secret containing
                          the client certificate.
                        properties:
                          name:
                            description: Name is the name of the secret
                            maxLength: 253
                            minLength: 1
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of the secret
                              If not specified, the namespace of the referrer is used
                            maxLength: 63
                            minLength: 1
                            type: string
                        required:
                        - name
                        type: object
                      clientKeyRef:
                        description: ClientKeyRef references a Secret containing the
                          client key.
                        properties:
                          name:
                            description: Name is the name of the secret
                            maxLength: 253
                            minLength: 1
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of the secret
                              If not specified, the namespace of the referrer is used
                            maxLength: 63
                            minLength: 1
                            type: string
                        required:
                        - name
                        type: object
                      insecureSkipVerify:
                        default: false
                        description: InsecureSkipVerify skips TLS certificate verification.
                        type: boolean
                      serverName:
                        description: ServerName is the expected server name for TLS
                          verification.
                        maxLength: 253
                        type: string
                    type: object
                required:
                - address
                - auth
                type: object
            required:
            - path
            - vaultConnection
            type: object
          status:
            description: VaultSecretStatus defines the observed state of VaultSecret
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition represents a condition of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        details about the transition
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: ObservedGeneration represents the .metadata.generation
                        that the condition was set based upon
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: Reason is a brief reason for the condition's last
                        transition
                      maxLength: 1024
                      minLength: 1
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconciledTime:
                description: LastReconciledTime is the last time the resource was
                  reconciled
                format: date-time
                type: string
              lastRefreshTime:
                description: LastRefreshTime is the last time the secret was refreshed
                  from Vault.
                format: date-time
                type: string
              lastVaultError:
                description: LastVaultError is the last error from Vault, if any.
                maxLength: 1024
                type: string
              nextRefreshTime:
                description: NextRefreshTime is the next scheduled refresh time.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed resource
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the resource
                enum:
                - Pending
                - Ready
                - Error
                - Reconciling
                - Degraded
                type: string
              secretVersion:
                description: SecretVersion is the version of the secret in Vault.
                maxLength: 256
                type: string
              targetSecretName:
                description: TargetSecretName is the name of the created/updated Kubernetes
                  secret.
                maxLength: 253
                type: string
              targetSecretNamespace:
                description: TargetSecretNamespace is the namespace of the created/updated
                  Kubernetes secret.
                maxLength: 63
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
