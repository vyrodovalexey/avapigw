---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: tlsconfigs.avapigw.vyrodovalexey.github.com
spec:
  group: avapigw.vyrodovalexey.github.com
  names:
    kind: TLSConfig
    listKind: TLSConfigList
    plural: tlsconfigs
    shortNames:
    - tlsc
    singular: tlsconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.certificate.notAfter
      name: NotAfter
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          TLSConfig is the Schema for the tlsconfigs API.
          TLSConfig defines TLS certificate configuration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TLSConfigSpec defines the desired state of TLSConfig
            properties:
              alpnProtocols:
                description: ALPNProtocols is the list of ALPN protocols.
                items:
                  type: string
                maxItems: 8
                type: array
              certificateSource:
                description: CertificateSource defines the source of the TLS certificate.
                properties:
                  secret:
                    description: Secret references a Kubernetes Secret containing
                      the certificate.
                    properties:
                      caKey:
                        description: CAKey is the key in the Secret containing the
                          CA certificate.
                        maxLength: 253
                        minLength: 1
                        type: string
                      certKey:
                        default: tls.crt
                        description: CertKey is the key in the Secret containing the
                          certificate.
                        maxLength: 253
                        minLength: 1
                        type: string
                      keyKey:
                        default: tls.key
                        description: KeyKey is the key in the Secret containing the
                          private key.
                        maxLength: 253
                        minLength: 1
                        type: string
                      name:
                        description: Name is the name of the Secret.
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the Secret.
                          If not specified, the namespace of the TLSConfig is used.
                        maxLength: 63
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  vault:
                    description: Vault references a Vault path containing the certificate.
                    properties:
                      caKey:
                        description: CAKey is the key in the Vault secret containing
                          the CA certificate.
                        maxLength: 253
                        minLength: 1
                        type: string
                      certKey:
                        default: certificate
                        description: CertKey is the key in the Vault secret containing
                          the certificate.
                        maxLength: 253
                        minLength: 1
                        type: string
                      keyKey:
                        default: private_key
                        description: KeyKey is the key in the Vault secret containing
                          the private key.
                        maxLength: 253
                        minLength: 1
                        type: string
                      mountPoint:
                        default: secret
                        description: MountPoint is the Vault mount point.
                        maxLength: 253
                        minLength: 1
                        type: string
                      path:
                        description: Path is the Vault path to the certificate.
                        maxLength: 1024
                        minLength: 1
                        type: string
                      vaultSecretRef:
                        description: VaultSecretRef references a VaultSecret for connection
                          details.
                        properties:
                          name:
                            description: Name is the name of the referent
                            maxLength: 253
                            minLength: 1
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - path
                    type: object
                type: object
              cipherSuites:
                description: CipherSuites is the list of allowed cipher suites.
                items:
                  type: string
                maxItems: 32
                type: array
              clientValidation:
                description: ClientValidation defines client certificate validation
                  (mTLS).
                properties:
                  caCertificateRef:
                    description: CACertificateRef references a Secret containing the
                      CA certificate.
                    properties:
                      name:
                        description: Name is the name of the secret
                        maxLength: 253
                        minLength: 1
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the secret
                          If not specified, the namespace of the referrer is used
                        maxLength: 63
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  enabled:
                    default: false
                    description: Enabled indicates whether client validation is enabled.
                    type: boolean
                  mode:
                    allOf:
                    - enum:
                      - Required
                      - Optional
                    - enum:
                      - Required
                      - Optional
                    default: Required
                    description: Mode defines the client validation mode.
                    type: string
                  subjectAltNames:
                    description: SubjectAltNames defines allowed subject alternative
                      names.
                    items:
                      description: SubjectAltNameMatch defines a subject alternative
                        name match
                      properties:
                        exact:
                          description: Exact is an exact SAN match.
                          maxLength: 253
                          type: string
                        prefix:
                          description: Prefix is a prefix SAN match.
                          maxLength: 253
                          type: string
                        regex:
                          description: Regex is a regex SAN match.
                          maxLength: 1024
                          type: string
                        suffix:
                          description: Suffix is a suffix SAN match.
                          maxLength: 253
                          type: string
                      type: object
                    maxItems: 16
                    type: array
                  trustedCAs:
                    description: TrustedCAs is a list of trusted CA certificates.
                    items:
                      description: SecretObjectReference identifies a Secret object
                        within a namespace
                      properties:
                        name:
                          description: Name is the name of the secret
                          maxLength: 253
                          minLength: 1
                          type: string
                        namespace:
                          description: |-
                            Namespace is the namespace of the secret
                            If not specified, the namespace of the referrer is used
                          maxLength: 63
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    maxItems: 16
                    type: array
                type: object
              maxVersion:
                allOf:
                - enum:
                  - TLS10
                  - TLS11
                  - TLS12
                  - TLS13
                - enum:
                  - TLS10
                  - TLS11
                  - TLS12
                  - TLS13
                default: TLS13
                description: MaxVersion is the maximum TLS version.
                type: string
              minVersion:
                allOf:
                - enum:
                  - TLS10
                  - TLS11
                  - TLS12
                  - TLS13
                - enum:
                  - TLS10
                  - TLS11
                  - TLS12
                  - TLS13
                default: TLS12
                description: MinVersion is the minimum TLS version.
                type: string
              rotation:
                description: Rotation defines certificate rotation configuration.
                properties:
                  checkInterval:
                    default: 1h
                    description: CheckInterval is the interval to check for certificate
                      expiration.
                    type: string
                  enabled:
                    default: true
                    description: Enabled indicates whether automatic rotation is enabled.
                    type: boolean
                  renewBefore:
                    default: 720h
                    description: RenewBefore is the duration before expiration to
                      renew the certificate.
                    pattern: ^([0-9]+(h|d))+$
                    type: string
                type: object
            required:
            - certificateSource
            type: object
          status:
            description: TLSConfigStatus defines the observed state of TLSConfig
            properties:
              certificate:
                description: Certificate contains information about the loaded certificate.
                properties:
                  dnsNames:
                    description: DNSNames is the list of DNS names in the certificate.
                    items:
                      type: string
                    maxItems: 32
                    type: array
                  fingerprint:
                    description: Fingerprint is the SHA-256 fingerprint of the certificate.
                    maxLength: 128
                    type: string
                  issuer:
                    description: Issuer is the certificate issuer.
                    maxLength: 1024
                    type: string
                  notAfter:
                    description: NotAfter is the certificate's not after time.
                    format: date-time
                    type: string
                  notBefore:
                    description: NotBefore is the certificate's not before time.
                    format: date-time
                    type: string
                  serialNumber:
                    description: SerialNumber is the certificate serial number.
                    maxLength: 256
                    type: string
                  subject:
                    description: Subject is the certificate subject.
                    maxLength: 1024
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition represents a condition of a resource
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned from one status to another
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        details about the transition
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: ObservedGeneration represents the .metadata.generation
                        that the condition was set based upon
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: Reason is a brief reason for the condition's last
                        transition
                      maxLength: 1024
                      minLength: 1
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: Type of condition
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 8
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconciledTime:
                description: LastReconciledTime is the last time the resource was
                  reconciled
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed resource
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the resource
                enum:
                - Pending
                - Ready
                - Error
                - Reconciling
                - Degraded
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
