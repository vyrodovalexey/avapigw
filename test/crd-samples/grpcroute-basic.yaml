apiVersion: avapigw.io/v1alpha1
kind: GRPCRoute
metadata:
  name: basic-grpc-route
  namespace: avapigw-test
spec:
  match:
    - service:
        prefix: api.v1
  route:
    - destination:
        host: basic-grpc-backend
        port: 8811
      weight: 100
  timeout: 30s
